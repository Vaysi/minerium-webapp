import {PaletteMode} from "@mui/material";

export const getDesignTokens = () => ({
    palette: {
        primary: {
            main: "#043180"
        },
        secondary: {
            main: "#CEA716"
        }
    },
});